module.exports.image = {
  fields:{
    
      url: (function(page, associatedModel , user){
        return true;
      }),
    
      name: (function(page, associatedModel , user){
        return true;
      }),
    
    
  }
}
