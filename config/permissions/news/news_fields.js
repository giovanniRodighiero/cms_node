module.exports.news = {
  fields:{
    
      asset: (function(page, associatedModel , user){
        return true;
      }),
    
    
      title: (function(page, associatedModel , user){
        return true;
      }),
    
      subtitle: (function(page, associatedModel , user){
        return true;
      }),
    
      content: (function(page, associatedModel , user){
        return true;
      }),
    
      published: (function(page, associatedModel , user){
        return true;
      }),
    
  }
}
