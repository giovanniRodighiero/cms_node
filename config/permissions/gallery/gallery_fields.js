module.exports.gallery = {
  fields:{
    
      asset: (function(page, associatedModel , user){
        return true;
      }),
    
    
      slug: (function(page, associatedModel , user){
        return true;
      }),
    
      news: (function(page, associatedModel , user){
        return true;
      }),
    
      products: (function(page, associatedModel , user){
        return true;
      }),
    
  }
}
