module.exports.product = {
  fields:{
    
      block_name: (function(page, associatedModel , user){
        return true;
      }),
    
      title: (function(page, associatedModel , user){
        return true;
      }),
    
      subtitle: (function(page, associatedModel , user){
        return true;
      }),
    
    
      price: (function(page, associatedModel , user){
        return true;
      }),
    
      icon: (function(page, associatedModel , user){
        return true;
      }),
    
  }
}
